schema:
  - graph/schema.graphqls

exec:
  layout: follow-schema
  dir: graph
  package: graph

model:
  filename: graph/model/models_gen.go
  package: model

resolver:
  layout: follow-schema
  dir: graph
  package: graph
